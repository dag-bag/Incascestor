/** @format */

import { atom } from "recoil";
export const CheckOutState = atom({
  key: "CheckOutState",
  default: [],
});

export const shipAddressAtom = atom({
  key: "shipAddressAtom",
  default: {},
});
